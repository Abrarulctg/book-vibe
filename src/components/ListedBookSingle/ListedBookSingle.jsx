
const ListedBookSingle = ({ book }) => {

    const { image, bookName, author, category, rating, tags, review, totalPages, publisher, yearOfPublishing } = book;
    return (
        <div>
            <h1>ListedBook Single</h1>
        </div>
    );
};

export default ListedBookSingle;